// import { } from '../actions/types';

const initialState = {
  loading: false,
  last_price: null,
  timestamp: null
}

export default function (state = initialState, action) {
  switch (action.type) {

    default:
      return state;
  }
}
