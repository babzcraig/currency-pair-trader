const BTC_API_URL = "https://api.bitfinex.com/v2/ticker/tBTCUSD";
// we will poll the API every 15 seconds for an update
const POLL_INTERVAL_IN_MS = 15000;

export { BTC_API_URL, POLL_INTERVAL_IN_MS };
